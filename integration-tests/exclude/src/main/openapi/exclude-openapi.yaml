---
openapi: 3.0.3
info:
  title: greeting-flow API
  version: "1.0"
paths:
  /hello_excluded:
    get:
      tags:
        - Excluded OpenAPI Resource
      operationId: hello_excluded
      responses:
        "200":
          description: OK
          content:
            text/plain:
              schema:
                type: string
