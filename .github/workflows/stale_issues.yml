name: Close stale issues and PRs
on:
  schedule:
    - cron: "0 12 * * *"

permissions:
  issues: write
  pull-requests: write

jobs:
  stale:
    runs-on: ubuntu-latest
    env:
      Maintainers: "@ricardozanini @hbelmiro"
      Stale_Message: "${{ env.Maintainers }} This is being labeled as Stale."
      Close_Message: "${{ env.Maintainers }} This is being closed due to inactivity."
    steps:
      - uses: actions/stale@v4.1.1
        with:
          exempt-issue-labels: "pinned"
          stale-issue-message: "${{ env.Stale_Message }}"
          stale-pr-message: "${{ env.Stale_Message }}"
          close-issue-message: "${{ env.Close_Message }}"
          close-pr-message: "${{ env.Close_Message }}"