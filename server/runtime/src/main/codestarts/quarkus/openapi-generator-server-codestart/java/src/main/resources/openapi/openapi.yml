openapi: 3.0.3
info:
  title: Generated API
  version: "1.0"
paths:
  /pets:
    get:
      responses:
        200:
          description: OK
          content:
            application/json: { }
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Pet'
      responses:
        200:
          description: OK
          content:
            application/json: { }
    delete:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Pet'
      responses:
        200:
          description: OK
          content:
            application/json: { }
components:
  schemas:
    Pet:
      properties:
        description:
          type: string
        name:
          type: string